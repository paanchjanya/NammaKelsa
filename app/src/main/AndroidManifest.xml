<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Required for Android 14+ to support partial access to photos/videos -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- ADD THIS LINE TO FIX THE WARNING -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/nk"
        android:label="@string/app_name"
        android:roundIcon="@drawable/nk"
        android:supportsRtl="true"
        android:theme="@style/Theme.NammaKelsa">

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- NEW: Role selection is now the entry point -->
        <activity
            android:name=".ui.RoleSelectionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ui.worker.WorkerProfileActivity"
            android:exported="false" />

        <activity
            android:name=".ui.worker.WorkerDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.customer.CustomerSearchActivity"
            android:exported="false" />

        <activity android:name=".ui.customer.AiSkillSuggesterActivity"
            android:exported="false"/>

    </application>

</manifest>